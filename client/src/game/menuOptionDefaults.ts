/**
 * Baked defaults for main-menu options (from local playtest preferences).
 * Used when localStorage has no saved value yet.
 */
export const MENU_BOTS_ENABLED_DEFAULT = true;
export const MENU_MUSIC_VOLUME_DEFAULT = 1;
