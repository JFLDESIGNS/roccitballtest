/**
 * Floor ring shown where beam use is temporarily blocked (explosions, knockdown, etc.).
 * Gameplay still uses `beamDenyZones.ts` when this is false — only the red ring is hidden.
 */
export const SHOW_BEAM_DENY_RINGS = false;
