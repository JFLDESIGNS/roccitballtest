/** Fan booths / glass — draw before combat VFX (beam, rockets, trails) */
export const FAN_BOOTH_RENDER_ORDER = 0;

/** Tractor beam, rockets, exhaust, held-ball ribbons */
export const COMBAT_VFX_RENDER_ORDER = 110;
